# Top-level readable declarations. Reproducible installs use requirements.lock
# (pip install -r requirements.lock). CI installs from the lock file.
akshare>=1.10.0
baostock>=0.8.9
pandas>=2.0.0
duckdb>=1.3.0,<2.0.0
pyarrow>=18.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
keyring>=25.0.0
filelock>=3.15.0
httpx>=0.27.0
